#!/bin/bash

export LC_ALL="C"
set -euo pipefail

. "$(dirname "$(realpath "$0")")/lib/defaults.sh"

TOPIC='schedule-sms'
USAGE="

  Listen on the request topic ($TOPIC)"

. "$(dirname "$(realpath "$0")")/lib/common.sh"

(($# == 0)) || usage_exit

kcat_listen
