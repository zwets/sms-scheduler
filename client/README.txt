SMS Scheduler Clients

This directory holds:

 - iam: everything for managing users, clients and roles (over REST)
 - rest: scripts to schedule, block, cancel, etc (over REST)
 - kafka: scripts to schedule, block and cancel (over the 'schedule-sms' topic)
 - lib: library, default settings, and public keys (see lib/README.txt)
